# Note grammar

@IMPORT ./query_string.cpp.hcg as Query

<> goal → note

<> note → type? Query::container_clause? \; tags? \; string?			↦c ^NOTE_Note

<> type → Query::number

<> tags → tag 														↦r ^NOTE_TagList
	│ tags , tag													↦r ^NOTE_TagList

<> tag → string \= Query::number 								↦r ^NOTE_TagNumber
	│ string \= string 											↦r ^NOTE_TagString
	│ string 													↦r ^NOTE_Tag

<> string → Query::string_data	 								↦r ^NOTE_String