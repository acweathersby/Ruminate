{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build prototype file",
            "command": "wasm-pack",
            "args": [
                "build",
                //"--debug",
                "--target",
                "web"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}