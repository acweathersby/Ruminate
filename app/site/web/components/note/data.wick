import {content} from "@parent";
import note from "./note.wick";
import * as rma from "/pkg/ruminate_prototype.js";

export default <span>
    <container data={content.data}>
        <span use-if={m.type == "text"}>{text}</span> 
        <span use-if={m.type=="note-insertion"}>
            <div class="query-lookup">
                discover
            </div>
            <container data={ [{id:ref}]}>
                <note/>
            </container>
        </span>
    </container>
</span>