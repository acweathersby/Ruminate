import {id, tag, name, buckets} from "@model";

import * as ruminate from "/pkg/ruminate_prototype.js";

export default <div class={ id >= 0 ? "tag" : "untagged"}>

    {name} {ruminate.get_notes_from_tag(tag).length}
    
    <container data={buckets}> <self/> </container>
</div>;

<style>

    root {
        position:relative;
        width: 120px;
        margin:2px;
        font-size: 12px;
        color:rgb(90,90,90);
    }
    
</style>
